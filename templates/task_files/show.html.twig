{% extends 'base.html.twig' %}

{% block title %}TaskFiles
{% endblock %}

{% block body %}
	<h1>TaskFiles</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ task_file.id }}</td>
			</tr>
			<tr>
				<th>FilesName</th>
				<td>{{ task_file.filesName }}</td>
			</tr>
		</tbody>
	</table>
	{# 
	    <a href="{{ path('app_task_files_index') }}">back to list</a>
	
	    <a href="{{ path('app_task_files_edit', {'id': task_file.id}) }}">edit</a>
	
	    {{ include('task_files/_delete_form.html.twig') }} #}
{% endblock %}
