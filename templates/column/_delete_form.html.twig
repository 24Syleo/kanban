<form method="post" action="{{ path('column.delete', {'id': project.id,'column_id': column.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ column.id) }}">
	<button class="btn btn-danger mt-2">Delete</button>
</form>
