{% extends 'base.html.twig' %}

{% block title %}Project
{% endblock %}

{% block body %}

	<div class="row">
		<div class="col-9">
			<h1>{{ project.title }}</h1>
		</div>
		<div class="col-3">
			<p class="fst-italic">{{ project.createdAt ? project.createdAt|date('Y-m-d') : '' }}</p>
		</div>
	</div>

	<div class="row">
		{% include 'column/index.html.twig' %}
	</div>

	<div class="row text-center">
		<div class="col-4">
			<a class="btn btn-primary" href="{{ path('home') }}">back to list</a>
		</div>
		<div class="col-4">
			<a class="btn btn-info" href="{{ path('project.edit', {'id': project.id}) }}">edit</a>
		</div>
		<div class="col-4">
			{{ include('project/_delete_form.html.twig') }}
		</div>
	</div>


{% endblock %}
