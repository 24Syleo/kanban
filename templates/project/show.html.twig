{% extends 'base.html.twig' %}

{% block title %}Project
{% endblock %}

{% block body %}

	<div class="row text-center mt-2">
		<div class="col-sm-6">
			<h1>{{ project.title }}</h1>
		</div>
		<div class="col-sm-6">
			<a class="btn btn-primary mt-2" href="{{ path('home') }}">back to list projects</a>
			<a class="btn btn-info mt-2" href="{{ path('project.edit', {'id': project.id}) }}">edit project</a>
			{{ include('project/_delete_form.html.twig') }}
		</div>
	</div>

	<div class="row mt-2 justify-content-around">
		{% include 'column/index.html.twig' %}
	</div>

{% endblock %}
