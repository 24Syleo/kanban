{% extends 'base.html.twig' %}

{% block title %}Edit Project
{% endblock %}

{% block body %}
	<h1>Edit
		{{ project.title }}</h1>

	{{ include('project/_form.html.twig', {'button_label': 'Update'}) }}

	<a class="btn btn-info mt-2" href="{{ path('project.show', {'id':project.id}) }}">back to
		{{ project.title }}</a>

	{{ include('project/_delete_form.html.twig') }}
{% endblock %}
