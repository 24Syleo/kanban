<form method="post" action="{{ path('task.delete', {'id': project.id, 'column_id': column.id,'task_id': task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
	<button class="btn btn-danger mt-3">Delete</button>
</form>
